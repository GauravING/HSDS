# Example frontend environment variables
# Uncomment and set one of these to override the API base URL used by the frontend during development.
# For Create React App (react-scripts) use REACT_APP_API
REACT_APP_API=http://localhost:8000

# For Vite (if used) use VITE_API
VITE_API=http://localhost:8000
